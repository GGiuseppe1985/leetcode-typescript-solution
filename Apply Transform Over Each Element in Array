function map(arr: number[], fn: (n: number, i: number) => number): number[] {
    let theArray=[]
    for(let i=0; i<arr.length; i++){
        theArray.push(fn(arr[i],i))
    }
    return theArray;

};
